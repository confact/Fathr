build_settings:
    verbose: false
    prefer_symlink: false

setup:
    composer:
        action: "install"
test:
    php_unit:
        config:
            - "phpunit.xml"
        run_from: "phpunit/"
        directory: "phpunit/Results/"

complete:

failure:
    email:
        committer: true