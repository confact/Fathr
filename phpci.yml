build_settings:
    verbose: false
    prefer_symlink: false

setup:
    - "php composer.phar install"
test:
    php_unit:
        config:
            - "phpunit/phpunit.xml"

complete:

failure:
    email:
        committer: true