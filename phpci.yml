build_settings:
    ignore:
        - "vendor"
        - "phpunit"

setup:
    composer:
        action: "install"
test:
    php_unit:
        config:
            - "phpunit/phpunit.xml"
        run_from: "phpunit/"
        coverage: "build/coverage"

complete:

failure:
    email:
        committer: true