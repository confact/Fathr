build_settings:
    verbose: false
    prefer_symlink: false

setup:
	- "php composer.phar install"
test:
    php_unit:
    	config:
            - "path/to/phpunit.xml"
        directory: "phpunit/"

complete:

failure:
    email:
        committer: true